export const metadata = {
  title: "Signin | NextStep AI",
  description: "Signin an existing account on NextStep AI.",
};

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
